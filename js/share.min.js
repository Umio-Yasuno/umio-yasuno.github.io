function toast_msg(){msg.classList.add(`toast-msg`);setTimeout(function(){msg.classList.remove(`toast-msg`);},1500);}
function copy_url(type){const msg=document.getElementById(`msg`);const title=document.title;const url=location.href;let txt=url;if(type==`title_url`){txt=title+"\n\n"+txt;}
navigator.clipboard.writeText(txt).then(function(){toast_msg();});}
function jump_tag(event){if(event.key!=`Enter`&&event.type!=`click`)
return;const tags_val=document.getElementById(`input-tag-search`);const list=document.getElementById(`tags-list`);const selector=list.querySelector(`[value="`+tags_val.value+`"]`);if(!selector)
return;const url="/tags/"+selector.dataset.url+"/";fetch(url,{method:`HEAD`,credentials:`same-origin`,referrerPolicy:`no-referrer`}).then(function(response){if(response.ok)
location.href=url;});}
function clear_value(){const tags_val=document.getElementById(`input-tag-search`);tags_val.value=``;}